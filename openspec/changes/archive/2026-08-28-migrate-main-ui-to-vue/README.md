# migrate-main-ui-to-vue

Rewrite main.ts's DOM-wired UI as Vue SFCs (App.vue + AppFooter.vue) and replace the text-only status message with an animated percentage progress report; src/lib/* stays untouched.
